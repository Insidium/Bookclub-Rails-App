<h2>Books</h2>
<%= image_tag 'clipart.png', alt:"Open book image"%>
<%if @books %>
    <% @books.each do |book|%>
        <p><%= link_to book.title, book_path(book) %></p>
        <p><%=book.author.name%></p>
    <%end%>
<%end%>
